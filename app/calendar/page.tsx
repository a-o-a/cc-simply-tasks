import { CalendarClient } from "@/components/calendar/calendar-client";

/**
 * 캘린더 페이지 — Phase 4 Step 6.
 * 월 보기 1차. 주/일 보기는 후속.
 */
export default function CalendarPage() {
  return <CalendarClient />;
}
